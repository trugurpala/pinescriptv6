{{NOTICE}}
# Pine Script Agent Kit unsupported retained legacy artifact for Zed

This generated `.zed/rules` file is an unsupported retained legacy artifact. It
is not a supported Zed instruction path. Under Zed's first-match precedence,
the project-root `.cursorrules` is found before `AGENTS.md` in this full
repository; use `.cursorrules` as the selected supported surface.

{{PROTOCOL}}

# Active sourced rules

{{RULES}}
